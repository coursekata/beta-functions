# show_cutoffs.R
# show_cutoffs() has graduated from beta and is now part of library(coursekata).
#
# If you have source("show_cutoffs.R") in your notebook, you can remove it —
# show_cutoffs() is available automatically after library(coursekata).
#
# See GRADUATED.md for the full list of functions that have moved into the package.

message("show_cutoffs() is now part of library(coursekata) — ",
        "you can remove source(\"show_cutoffs.R\") from your notebook.")
