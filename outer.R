# outer.R
# outer() has graduated from beta and is now part of library(coursekata).
#
# If you have source("outer.R") in your notebook, you can remove it —
# outer() is available automatically after library(coursekata).
#
# See GRADUATED.md for the full list of functions that have moved into the package.

message("outer() is now part of library(coursekata) — ",
        "you can remove source(\"outer.R\") from your notebook.")
